# توثيق نظام اليوسف لإدارة المقاولات

**آخر تحديث:** أغسطس 2026
**الهدف من الملف ده:** تفهم شكل النظام في ربع ساعة، من غير ما تحتاج تقرا آلاف أسطر الكود. سواء إنت رجعتله بعد شهور، أو مبرمج جديد هيشتغل عليه، ابدأ من هنا.

---

## 1. الخريطة العامة

النظام عبارة عن **تطبيق سطح مكتب حقيقي** (مش موقع، ومش يحتاج إنترنت للاستخدام اليومي)، مبني بتقنية اسمها **Tauri** — وهي غلاف بيحوّل صفحة ويب عادية لبرنامج ويندوز مثبّت.

**3 مكونات رئيسية:**

| المكوّن | إيه هو | فين |
|---|---|---|
| الواجهة والمنطق | كل الشاشات، الحسابات، التقارير — كود HTML/JavaScript في ملف واحد | `dist/index.html` |
| الغلاف (Tauri) | كود بسيط بلغة Rust، وظيفته الوحيدة إنه يوصّل الواجهة بقاعدة البيانات وبفتح نافذة البرنامج | `src-tauri/` |
| قاعدة البيانات | كل بياناتك الفعلية (عملاء، عقود، مستخلصات...) — ملف SQLite حقيقي | على جهازك، مش في المشروع |

**ليه الشكل ده تحديدًا؟** عشان الجزء الأصعب (المنطق المالي والحسابات) موجود في مكان واحد بسيط (ملف HTML/JS)، وأي مبرمج ويب عادي (مهارة شائعة جدًا) يقدر يتعامل معاه. جزء الـ Rust (الأصعب توفرًا) اتقصد يفضل صغير جدًا ومبني على إضافة رسمية موثّقة (`tauri-plugin-sql`)، مش كود مخترع من الصفر.

---

## 2. الجداول والعلاقات (قاعدة البيانات)

**تحديث مهم:** بقرار صريح، النظام بقى **من غير بنود/كميات تفصيلية خالص** — العقد وأمر الإسناد والمستخلص كل واحد فيهم رقم قيمة واحد تدخله يدويًا، مش جدول بنود. جداول البنود القديمة (`contract_boq_items`, `assignment_items`, `extract_items`, `wbs_codes`) **لسه موجودة في قاعدة البيانات لكن مش بتتملى بيانات جديدة** — سيبناها كده بدل ما نحذفها (`DROP TABLE`) تجنبًا لأي مخاطرة، وأي بيانات قديمة كانت فيها بنود اتجمعت في رقم إجمالي واحد تلقائيًا عند التحديث.

```
العملاء (clients)
  └─ العقود (contracts) — قيمة إجمالية واحدة
       ├─ الملاحق (annexes)
       └─ أوامر الإسناد (assignments) — قيمة إجمالية واحدة
            └─ المستخلصات (extracts) — قيمة الفترة الحالية

مقاولو الباطن (subcontractors)
  └─ عقود الباطن (sub_contracts) ─── بنود عقد الباطن (sub_contract_items)
       └─ جدول الدفعات المخطط (sub_contract_payment_schedule)
  └─ الدفعات الفعلية (sub_payments)

الموردون (suppliers)
  ├─ المشتريات (purchases)
  └─ دفعات الموردين (supplier_payments)

إعدادات: بيانات الشركة (company_settings) | app_meta (بيانات داخلية للبرنامج)
```

**القاعدة الذهبية:** حذف أي عنصر أب (زي عقد) بيمسح **كل** اللي تحته تلقائيًا (أوامر إسناد، مستخلصات) — قاعدة البيانات نفسها بتضمن كده (مش كود بيتحقق يدويًا)، فمفيش خطر إن حاجة "تتيتم" وتفضل معلّقة.

---

## 3. قواعد مالية مهمة (لازم أي حد يعدّل في الكود يعرفها)

### أ. الضمان بيتحسب على "قيمة مجمّدة"، مش القيمة الحالية
كل أمر إسناد فيه حقل منفصل اسمه **"القيمة الأساسية لحساب الضمان"**. أول ما يتحدد، أي تعديل بعد كده على قيمة الأمر **مبيغيّرش** حساب الضمان القديم. لو عدّلت القيمة وعايز الضمان يتغيّر فعلاً، لازم تعدّل القيمة دي يدويًا من فورم الأمر.

**ليه؟** لأن في الواقع، الضمان بيتحصّل فعليًا مرة واحدة، ومبيتغيّرش لمجرد إن قيمة الأمر اتعدّلت في البرنامج بعد كده. ده اكتُشف بعد ما حصل تعارض حقيقي بين المحصّل فعليًا والمحسوب في البرنامج.

### ب. طريقتين لسداد ضمان أمر الإسناد
- **دفعة مقدمة**: بتتسدد مرة واحدة (شيك/نقدي/خطاب ضمان)، برّه المستخلصات خالص.
- **خصم من المستخلص الأول**: بتتخصم تدريجيًا من المستخلصات.

أي تقرير أو حساب لازم ياخد بالطريقتين في الاعتبار — فيه دالة جاهزة اسمها `assignmentTotalRetained()` بتعمل كده صح، استخدمها بدل ما تحسب يدويًا.

### ج. VAT بيتضاف بطرق مختلفة حسب السياق
بعض الحسابات (زي ضمان "الخصم من المستخلص") بتحسب شامل الضريبة، وبعضها (زي "الدفعة المقدمة") من غير ضريبة خالص. لو مش متأكد، دوّر على دالة موجودة بالفعل بدل ما تكتب حساب جديد.

---

## 4. أماكن الملفات المهمة

| إيه | فين |
|---|---|
| كود البرنامج (الواجهة) | `dist/index.html` |
| قاعدة البيانات الفعلية على جهازك | `%APPDATA%\com.alyousef.erp\data.db` |
| نسخة احتياطية قديمة (JSON، من قبل SQLite) | `%APPDATA%\com.alyousef.erp\data.json` |
| تصميم الجداول (schema) | `src-tauri/migrations/*.sql` |
| اختبارات تلقائية للتأكد إن قاعدة البيانات شغّالة صح | `dev-tests/` |
| إعدادات البرنامج (اسم، أيقونة، حجم النافذة) | `src-tauri/tauri.conf.json` |
| المستودع على GitHub | `github.com/DERAA2025/AlYoussef_ERP` |

**نسخة احتياطية يدوية:** زرار "تصدير نسخة احتياطية (JSON)" جوه البرنامج (الإعدادات → البيانات). استخدمه بانتظام واحفظ الملف برّه الجهاز (فلاشة/Drive).

---

## 5. تاريخ القرارات المهمة (وليه)

| القرار | السبب |
|---|---|
| ملف HTML واحد بدل تطبيق ويب بسيرفر | مستخدم واحد، جهاز واحد، مفيش حاجة لدخول عن بُعد — سيرفر كان تكلفة زيادة من غير فايدة |
| Tauri بدل Electron | نفس الفكرة، بس حجم تثبيت أصغر بكتير |
| SQLite بدل ملف JSON | ملف JSON كان بيتكتب بالكامل في كل حفظة — أي قطع كهرباء وقت الحفظ ممكن يتلف الملف كله. SQLite بيضمن إن كل حفظة تتم بالكامل أو محصلش خالص |
| نقل تلقائي من JSON لـ SQLite | عشان محدش يضطر يدخل بياناته يدوي تاني — البرنامج بيتأكد إن الأرقام متطابقة قبل وبعد النقل |
| هرمية WBS بـ3 مستويات | كانت مستوى واحد بس، والتقارير احتاجت تجميع حسب فئات رئيسية |
| القيمة المجمّدة لحساب الضمان | اكتُشفت مشكلة حقيقية: تعديل كميات أمر إسناد كان بيغيّر حسابات ضمان قديمة اتحصّلت فعلاً في الواقع |
| إزالة البنود/الكميات التفصيلية (وWBS معاها) من العقود وأوامر الإسناد والمستخلصات، والاكتفاء بقيمة إجمالية واحدة لكل واحد | قرار صريح لتبسيط الاستخدام اليومي — تتبع البند تلو الآخر كان يسبب تعقيد وأخطاء متكررة (كميات بتتغيّر بالغلط، بنود بتتوه بين المستخلصات) أكبر من الفايدة منه |

---

## 6. لو حبيت تعمل نسخة جديدة (بعد أي تعديل في الكود)

1. حدّث الملفات على GitHub (`dist` و/أو `src-tauri` حسب مكان التعديل).
2. تبويب **Actions** في المستودع هيبني installer جديد تلقائيًا (10-15 دقيقة).
3. نزّل الملف من **Artifacts**، وثبّته فوق النسخة القديمة.
4. لو التعديل غيّر شكل قاعدة البيانات (أضاف جدول/عمود جديد)، لازم يتضاف كملف migration جديد في `src-tauri/migrations/` برقم تسلسلي جديد، ويتسجّل في `src-tauri/src/main.rs`.

**قبل أي تحديث كبير:** خد نسخة احتياطية يدوية من زرار التصدير جوه البرنامج، احتياطًا.
